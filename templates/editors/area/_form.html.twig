<table>
    <tr>
        <td>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('Speichern') }}</button>
            {{ form_end(form) }}
        </td>
        <td>
            <h3>Vorschau:</h3>
            Name:<br />
            <div id="preview_name">
            </div>
            Beschreibung: <br />
            <div id="preview_descr">
            </div>
        </td>
    </tr>
</table>
<script>
    $(document).ready(function () {
        var previewName = new formatting.Preview(document.getElementById('area_coloredName'), document.getElementById('preview_name'));
        var previewDescr = new formatting.Preview(document.getElementById('area_description'), document.getElementById('preview_descr'));
    })
</script>