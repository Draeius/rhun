<h1>User</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ user.username }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Gems</th>
            <td>{{ user.gems }}</td>
        </tr>
        <tr>
            <th>MaxChars</th>
            <td>{{ user.maxChars }}</td>
        </tr>
        <tr>
            <th>ValidationCode</th>
            <td>{{ user.validationCode }}</td>
        </tr>
        <tr>
            <th>Validated</th>
            <td>{{ user.validated ? 'Yes' : 'No' }}</td>
        </tr>
        <tr>
            <th>Birthday</th>
            <td>{{ user.birthday ? user.birthday|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Ban</th>
            <td>{{ user.ban ? user.ban|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>BanReason</th>
            <td>{{ user.banReason }}</td>
        </tr>
        <tr>
            <th>Template</th>
            <td>{{ user.template }}</td>
        </tr>
        <tr>
            <th>AcceptTerms</th>
            <td>{{ user.acceptTerms ? 'Yes' : 'No' }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('user_index') }}">back to list</a>

<a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

{{ include('editors/user/_delete_form.html.twig') }}

